package com.mindbody.hybrid

import androidx.fragment.app.Fragment
import androidx.compose.ui.platform.ComposeView

class HybridFragment : Fragment() {
    fun attach(view: ComposeView) { /* hosts compose */ }
}
